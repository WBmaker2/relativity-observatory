## 2026-09-23

- 상대성 관측소 P0 첫 화면을 만들었다. 사건 설정, 장면, 좌표 표, 시공간 도표, 기록 흐름이 한 페이지에서 이어진다.
- β=0.6 예제(A·B 동시가 S′에서 1.5초 차이로 벌어짐), 빛 도착 시간과 사건 시간 분리, 간격 분류를 수치와 도표로 함께 보여준다.
- 입력 범위를 벗어나면 이전 값을 유지하고 이유를 알려준다. 키보드와 320px 작은 화면, 움직임 줄이기 설정을 함께 고려했다.
- 배포 준비를 시작했다. 사용 설명서와 검증 기록, 배포 점검표를 만들었다. 아직 배포하지 않았다.
- 빈칸 입력과 깨진 저장 기록에도 화면이 깨지지 않게 다듬었다. 작은 화면 320·360·768·1280px과 하위 경로에서도 동작을 확인했다. 입력 반응은 100ms 안이다.
- 교과 검토를 마쳤다. 수치를 직접 다시 계산해 화면과 일치함을 확인했고, 시간 지연 공식 남용 금지 한 줄을 화면에 덧붙였다. 배포를 막을 문제는 없었다.
- 깃허브에 공개하고 페이지스 배포를 마쳤다. 공개 주소에서 수치·화면·업데이트 내역이 정상 동작함을 직접 확인했다.
- 움직임 줄이기·고대비·접근성 트리를 실측했다. 펄스 대체·시스템 색상 전환·대비율 AA·컨트롤 이름 23개 전부를 확인했고, 실제 스크린리더 낭독은 사람 확인으로 남겼다.
- 교과 권고에 따라 고유시간 예시(β=0.6에서 이동 시계 1초는 S에서 1.25초)를 화면 안내에 한 줄 덧붙였다.
- 사건을 2~4개로 늘렸다. C·D 버튼으로 탐구용 사건을 추가하면 좌표·도착·전 쌍 간격·도표가 함께 갱신된다. 기본값 C(1,0)·D(-1,0)는 빛꼴 쌍을 보여준다.
- 성취기준 매핑 검토를 마쳤다. 2015 물리학Ⅰ에는 직접 부합, 2022 물리학에서는 동시성·정량 계산이 심화임을 기록했다. 원문 확정은 교육과정정보센터 대조가 필요하다.
- P0 도입 삽화 3장의 자산 명세를 만들었다. 생성 도구가 없어 제작은 사용자 협의 후로 남겼다.
- P1(길이·고유시간·조각별 관성계) 별도 설계를 마쳤다. 가속·쌍둥이 해석 결론은 별도 설계 전까지 유보한다.
- P1을 구현했다. 길이 동시 측정(L₀=2, β=0.6 → 1.6), 고유시간, 2구간 왕복 여정(좌표시 2.5초·고유시간 2초)을 화면에서 바로 잴 수 있다. 테스트 20개 통과.
- 성취기준 원문을 NCIC에서 직접 확인했다. 2022 물리학 시간 팽창·길이 수축 기준과 해설(동시성·정량 계산 제외)을 확정했고, 2015 코드 직접 열람은 후속 과제로 남겼다.
- 도입 삽화 3장을 Flow에서 생성해 넣었다. 1장은 검수 탈락 후 다시 만들어 통과했고, 수치·수식 없이 가벼운 WebP로 넣었다.
- 학생 입장에서 전체 흐름을 읽어보고 4건을 고쳤다. 비교 카드의 기록 오해 문구, P1 길이 측정 기준계 표기(S에서 동시 측정), 기준 쌍 A–B 표기, 장면 범례의 사각형 구분이다.

## 2026-09-24

- 중·고등학생 학습 흐름을 다듬었다. 예측 전 계산 결과를 숨기고, 예측→장면 관찰→비교→기록 순서로 진행하게 했다. 예제 재설정은 재생·예측·설명·입력·시간·기준계를 초기화한다.
- 모바일 표의 가로 이동 안내, 학생용 예측·비교 상태 문구, β 범위 오류에서 입력값과 계산 적용값 설명을 추가했다. 심화 계산은 핵심 흐름 저장 뒤 열 수 있도록 접었고, 저장 뒤 학습 요약과 새 β 예측 버튼을 제공한다.
