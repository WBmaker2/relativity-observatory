# 12-P1. 상대성 관측소 연속체 (길이·고유시간·조각별 관성계)

- 프로젝트: `relativity-observatory` / 상태: 설계 (구현 전) / P0 완료 후 착수
- [P0 설계](./12-relativity-observatory.md) · [공통 원칙](./00-shared-design-principles.md)
- 제외 유지: 일반상대론, 블랙홀, 광속 초과, 사실적 상대론 광학 렌더링.
- 가속·쌍둥이 역설의 해석 결론은 별도 설계 전까지 내지 않는다 (계산만 제공하고 결론은 유보).

## 1. 질문·차별성

‘움직이는 자의 길이는 누가, 언제 재는가?’ 길이 측정의 동시성 조건, 같은 시계가 재는 고유시간, 방향을 바꾸는 여정의 조각별 계산을 분리해 탐구한다. P0가 ‘사건 좌표의 변환’이었으면, P1은 ‘측정 조건의 차이’다.

## 2. 학습 목표·오개념

- 길이 수축은 같은 시각에 잰 두 끝의 거리라는 조건을 붙여 설명한다 (L′=L₀/γ는 동시 측정에서만).
- ‘물체가 눌려 짧아졌다’는 오해를 방지한다. 같은 막대를 재는 조건이 달라진 것이다.
- 고유시간은 같은 시계가 겪은 시간이며, 서로 다른 장소의 임의 사건에 Δt=γΔτ를 적용하지 않는다.
- 조각별 관성계는 각 구간이 등속이며, 속도가 바뀌는 이음매는 이상화(순간 전환)임을 표시한다.
- 왕복 여정의 고유시간 합이 좌표시보다 짧다는 계산은 보여주되, ‘누가 젊어지는가’의 해석·쌍둥이 역설 결론은 별도 설계 전까지 다루지 않는다.

## 3. 30분 흐름 (P0 이후 차시 가정)

1. 5분: β=0.6 막대(L₀=2광초)의 양끝을 S′에서 ‘동시에’ 재면 얼마인지 예측한다.
2. 10분: 동시 측정 조건(t₁′=t₂′)을 적용해 L′=1.6광초를 확인하고, 동시가 아니면 값이 달라짐을 본다.
3. 5분: 같은 이동 시계의 1초가 S에서 1.25초로 측정됨을 확인한다.
4. 5분: 왕복 2구간(나감 +0.6, 돌아옴 −0.6) 여정의 고유시간 합을 구한다 (결론 유보).
5. 5분: ‘길이 수축·시간 지연은 측정 조건의 차이’라는 한 줄 설명을 저장한다.

## 4. 화면·상태 (P0 확장, 새 화면을 늘리지 않는다)

| P0 화면 | P1 추가 | 행동 |
|---|---|---|
| 사건 설정 | 막대 길이 L₀, 구간별 β 목록 | 예측·시작 |
| 장면 | 막대 양끝 + 동시 절단선 표시 | 정지·시간 이동 |
| 좌표 | 측정 쌍(t₁′=t₂′ 강제 표시) | 기준계 선택 |
| 도표 | 세계선 구간별 색·모양 구분 (색만 아님) | 사건 강조 |
| 기록 | 길이·고유시간·구간 합 | 설명 저장 |

- 길이 측정 모드에서는 측정 쌍의 t′ 동시성을 강제하고, 강제값을 화면에 표시한다.
- 구간 전환점은 마름모+‘순간 전환(이상화)’ 표기로 구분한다.

## 5. 모델·단위·규약 (c=1 유지)

- 길이: S′ 정지 막대 양끝 x₁′,x₂′ (L₀=x₂′−x₁′). S에서 t₁=t₂ 조건으로 Lorentz 역변환한 거리 L=L₀/γ. 수치 예제: L₀=2, β=0.6 → L′=1.6.
- 동시 조건 위반 검사: |t₁′−t₂′|>eps면 길이로 인정하지 않고 ‘동시 측정 아님’ 표시.
- 고유시간: 등속 구간 Δτ=Δt√(1−β²). 예제: S′ 시계 1초 ↔ S 1.25초 (P0 테스트와 동일식).
- 조각별: 구간 i마다 β_i, 좌표시 Δt_i. 이음매 연속성 x(t) 연속 확인. 고유시간 합 ΣΔτ_i. 왕복 예제: 각 구간 Δt=1.25초, β=±0.6 → 구간당 Δτ=1초, 합 2초 < 좌표시 합 2.5초. 해석 결론은 표시하지 않는다.
- 입력 범위: L₀∈(0,10]광초, 구간 수 2~3개, β 범위는 P0와 동일.

## 6. 데이터·평가

- Rod: id, restLength, endpoints. Measurement: rodId, frameId, t1, t2, simultaneous, length.
- Segment: id, beta, dtCoordinate. Journey: segments[], junctionNote.
- 자동 판정: L′/L₀=1/γ, 동시 조건, 고유시간 적분, 이음매 연속성. 서술 평가는 ‘조건-결과 구분’에 집중.

## 7. 검증·완료

- β=0이면 L′=L₀, Δτ=Δt.
- L₀=2, β=0.6 → 1.6 고정 테스트. 동시 위반 시 거부 테스트.
- 구간 합이 각 구간 고유시간 합과 일치, 좌표시 합보다 작음(왕복 예제) 테스트.
- 키보드 구간·길이 입력, 320px 표, reduced-motion, 2D 대체 확인.
- 완료: 길이·고유시간·구간 합이 모든 화면에서 일치하고 위 검증을 통과한다. 쌍둥이 해석 문구가 화면·문서에 없음을 검수한다.

## 8. 구현 순서

- M6: length.ts (동시 측정 + L₀/γ) → M7: propertime.ts (적분) → M8: segments.ts (이음매·합) → M9: 화면 확장·설명과 경계 검수.
- 모듈: length.ts, propertime.ts, segments.ts + 기존 화면 확장. 각각 500줄 미만.
